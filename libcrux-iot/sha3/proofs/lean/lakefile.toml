name = "LibcruxIotSha3"
version = "0.1.0"
defaultTargets = ["LibcruxIotSha3"]

[[lean_lib]]
name = "LibcruxIotSha3"
# No `globs`: the library is driven by the `LibcruxIotSha3` root module's import
# tree (below) rather than by globbing every file on disk. This keeps the
# generated `Extraction/ProofObligations.lean` (sorry-filled, @[spec]-tagged, and
# imported by nothing) OUT of the build while leaving the file on disk. The root
# imports the import-DAG sink `Verification.ProofObligations`, which transitively
# pulls in every real proof module.
globs = ["LibcruxIotSha3"]

[[require]]
name = "Hax"
git = "https://github.com/cryspen/hax-lean"
rev = "v0.3.17"

# The machine-extracted FIPS-202 hacspec (crate `hacspec_sha3`, specs/sha3 in
# celabshq/libcrux); the proofs reference `hacspec_sha3.*`.
# Pinned by COMMIT SHA, not by branch name: daba41a6 is the specs/sha3
# hax v0.4.0-rc.2 bump (hax-lean v0.3.17), the same commit Cargo.toml pins for
# the `hacspec_sha3` crate. NOT YET PUSHED to celabshq/libcrux: until it is,
# lake cannot fetch this rev on a fresh machine -- use the local-path form
# (scratchpad lakepath.py) there, as this working tree does between commits.
[[require]]
name = "HacspecSha3"
git = "https://github.com/celabshq/libcrux"
rev = "daba41a6bffa3bbdbb0e3f5710590086f95da1d0"
subDir = "specs/sha3/proofs/lean"
