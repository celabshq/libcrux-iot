[workspace]
members = ["ml-dsa", "ml-kem", "sha3", "aes", "p256"]
resolver = "3"

[workspace.package]
version = "0.0.3"
authors = ["Cryspen"]
license = "Apache-2.0 OR MIT"
homepage = "iot.cryspen.com"                          # TODO: Set this up
edition = "2021"
repository = "https://github.com/cryspen/libcrux-iot"
readme = "README.md"

[workspace.dependencies]
libcrux-kats = { git = "https://github.com/cryspen/libcrux.git" }
libcrux-secrets = { version = "0.0.6" }
libcrux-macros = { version = "0.0.3" }
libcrux-traits = { version = "0.0.8" }
libcrux-hacl-rs = { version = "0.0.5" }
libcrux-sha2 = { version = "=0.0.8" }

hax-lib = { version = "0.4.0" }


[workspace.lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(hax)',
    'cfg(eurydice)',
    'cfg(doc_cfg)',
    'cfg(hax_backend_lean)',
] }

[profile.release-debug]
inherits = "release"
debug = true
strip = false
